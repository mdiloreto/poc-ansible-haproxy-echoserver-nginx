haproxy:
image: {{ haproxy_image }}
container_name: haproxyserver
ports:
  - "80:80"
networks:
  - {{ network }}
volumes:
  - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg

networks:
  app_net:    
    external: true