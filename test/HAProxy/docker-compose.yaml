version: '3.8'
services:
  haproxy:
    image: haproxy:3.0.3
    ports:
      - "80:80"
    # volumes:
    #   - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg
    networks:
      - app_net

networks:
  app_net:
    driver: bridge