version: '3.8'
services:
  haproxy:
    image: haproxy:3.0.3
    container_name: haproxyserver
    ports:
      - "80:80"
      - "8404:8404"
    networks:
      - app_net
    volumes:
      - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg

networks:
  app_net:
    external: true
